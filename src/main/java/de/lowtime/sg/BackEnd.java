package de.lowtime.sg;

import java.util.*;

public class BackEnd {

    public static Map<UUID, UUID> tpRequest = new HashMap<UUID, UUID>();
    public static Map<UUID, TeleportType> tpType = new HashMap<UUID, TeleportType>();

}